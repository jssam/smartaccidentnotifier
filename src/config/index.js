import {
    API_BASE_URL,
    CLIENT_ID,
    CLIENT_SECRET,
    VERSION
  } from '../../env.json';
  
  const config = {
    API_BASE_URL,
    CLIENT_ID,
    CLIENT_SECRET,
    VERSION
  };
  
  export default config;
  